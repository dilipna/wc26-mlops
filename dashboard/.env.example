# Live Inference Console + serving-status pill: the real deployed FastAPI
# serving layer (src/serving/app.py). Point this at the Render deployment
# in production (Vercel project env var), or a local `uvicorn` during dev.
NEXT_PUBLIC_SERVING_API_URL=http://localhost:8000
