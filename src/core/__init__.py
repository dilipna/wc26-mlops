"""Sport-agnostic core: the `Sport` interface every sport plugin implements.

See `src.core.sport` for the interface itself and `src.sports.football` for
the first (and currently only) implementation.
"""
